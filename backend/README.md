# Node.js + Express + TypeScript + MongoDB Starter

## Setup

1. Install dependencies:
   - `npm install`
2. Create `.env` from `.env.example` and update values.
3. Run in development:
   - `npm run dev`

## Build & run

- `npm run build`
- `npm start`

